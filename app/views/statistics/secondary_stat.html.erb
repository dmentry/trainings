<%= link_to(image_pack_tag("home.png", alt: 'home', class: 'img-home'), trainings_path, class: 'for-mobile buffer_top_home') %>

<div class="container my-bg-color without_borders">
  <div class="row additional_buffer_top">
    <div class="col-lg-12 order-lg-2 mb-2 mt-1 no-paddings_second_stat">
        <h6 class="center text_size head_color head_color top_buffer_statistics">
          Тренировок с <%= "#{ @all_tr_by_month_formatted.first[0] }: #{@all_trainings_by_user.count}. Среднее количество тренировок в месяц: #{ @average_in_month }. #{@overall_training_time}" %>
        </h6>
        <% if @worse_trainings_ration > 0 %>
          <h6 class="center text_size mt-0 head_color head_color">
            Текущий месяц по тренировкам лучше, чем <%= @worse_trainings_ration %>% предыдущих месяцев.
          </h6>
        <% end %>
      <div class="container my-bg-color without_borders">
        <div class="row">
          <div class="col-lg-12 order-lg-1 my-2 no-paddings">
            <!-- <div class="ex_buffer_both_my"> -->
              <% if @all_tr_by_month_formatted.present? %>
                <div class="center mt-0 bg-light">
                  <b>Количество тренировок по месяцам</b>
                  <%= column_chart(@all_tr_by_month_formatted, {theme: 'palette4'}) %>
                </div>
              <% end %>

              <% if @tr_by_label_chart.present? %>
                <div class="bg-light">
                  <div class="center">
                    <div class="mt-3">
                      <b>Количество проведенных упражнений</b>
                      <%= bar_chart(@tr_by_label_chart, { height: 500, theme: 'palette7' }) %>
                    </div>
                  </div>
                </div>
              <% end %>
              
              <% if @exercises_max_results.present? %>
                <div class="bg-light">
                  <div class="center">
                    <div class="mt-3">
                      <b>Максимальное количество повторов в каждом упражнении</b>
                      <%= bar_chart(@exercises_max_results, { height: 500, theme: 'palette7' }) %>
                    </div>
                  </div>
                </div>
                  <%# if ex[0].match?(/бег|лыжи|Бег|Лыжи/) %>
                    <%# summ = ex[1] %>
                  <%# else %>
                    <%# summ = ex[1].to_i %>
                  <%# end %>
                  <!-- # -->
                  <%# if list != @exercises_list.last %>
                    <!-- | -->
                  <%# end %>
              <% end %>

            <!-- </div> -->
          </div>
        </div>
      </div>

    </div>
  </div>
</div>
