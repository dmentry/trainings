<%= link_to 'На главную', trainings_path, class: "h1 btn-sm btn-secondary mt-1 buffer-bottom for-mobile" %>
<h1 class="center text_size mt-1">Редактировать название тренировки: <%= @exercise_name_voc.label %></h1>
<div class="container my-bg-color">
  <div class="row my-3">
    <div class="col-sm-12 col-md-8">
      <%= render 'form', exercise_name_voc: @exercise_name_voc || @exercise_name_voc = current_user.exercise_name_vocs.build(exercise_name_voc_params) %>
      
      <div class="my-3">
        <%= link_to 'Удалить', @exercise_name_voc, method: :delete, data: { confirm: 'Are you sure?' } %> |
        <%= link_to 'Обратно', exercise_name_vocs_path %>
      </div>
    </div>
  </div>
</div>
